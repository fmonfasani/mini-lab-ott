apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: origin-claim0
  name: origin-claim0
  namespace: edge
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
